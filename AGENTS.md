# REALPOLITICS — Repository Guidelines

This repository is synced bidirectionally with the connected development
environment. Avoid rewriting published git history (force pushes, rebases or
amends of commits that are already pushed), and keep the `main` branch in a
working state.
