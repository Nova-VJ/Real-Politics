import { createFileRoute,Link } from '@tanstack/react-router';import {PageHeader} from '@/components/app-shell';import {paths} from '@/data/content';import {ArrowRight,Clock} from 'lucide-react';
export const Route=createFileRoute('/paths')({head:()=>({meta:[{title:'Learning Paths — REALPOLITICS'},{name:'description',content:'Follow curated journeys through economics, government, history, finance and critical thinking.'},{property:'og:title',content:'Learning Paths — REALPOLITICS'},{property:'og:description',content:'Structured journeys through connected knowledge.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <><PageHeader eyebrow="Structured learning" title="Learning paths" description="Follow a carefully sequenced journey. Each idea prepares you for the next."/><div className="mx-auto max-w-6xl px-5 py-12 lg:px-8"><div className="divide-y divide-border border-y border-border">{paths.map((p,i)=><Link key={p.slug} to="/path/$slug" params={{slug:p.slug}} className="group grid gap-4 py-7 md:grid-cols-[80px_1fr_auto] md:items-center"><span className="font-serif text-4xl text-muted-foreground">0{i+1}</span><div><span className="eyebrow">{p.category}</span><h2 className="mt-2 font-serif text-2xl">{p.title}</h2><p className="mt-2 flex gap-4 text-sm text-muted-foreground"><span>{p.lessons} lessons</span><span className="flex items-center gap-1"><Clock className="size-4"/>{p.hours} hours</span></p></div><ArrowRight className="size-5 text-primary transition group-hover:translate-x-1"/></Link>)}</div></div></>})
