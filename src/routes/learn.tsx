import {createFileRoute} from '@tanstack/react-router';import {PageHeader,SectionTitle} from '@/components/app-shell';import {CourseCard} from '@/components/content-cards';import {courses} from '@/data/content';export const Route=createFileRoute('/learn')({head:()=>({meta:[{title:'Learn — REALPOLITICS'},{name:'description',content:'Courses designed to build understanding from first principles.'},{property:'og:title',content:'Learn — REALPOLITICS'},{property:'og:description',content:'Build understanding with connected courses.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:()=> <><PageHeader eyebrow="Learn" title="Build a durable understanding" description="Courses that begin with intuition, then connect ideas across disciplines."/><div className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><SectionTitle title="Your courses"/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{courses.map(c=><CourseCard key={c.slug} course={c}/>)}</div></div></>})
